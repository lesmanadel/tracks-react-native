import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const TrackDataScreen = () => {
  return <Text style={{ fontSize: 48 }}>TrackDataScreen</Text>;
};

const styles = StyleSheet.create({});

export default TrackDataScreen;
